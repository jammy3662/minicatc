#ifndef SYMBOL_DEF
#define SYMBOL_DEF

#include <vector>
#include <map>
#include <string>

#include "cext/ints.h"
//#include "cext/list.h"

// TODO: replace C++ STL functionality
template <typename T>
using Array = std::vector <T>;

template <typename Key, typename Val>
using Table = std::multimap <Key, Val>;

using string = std::string;

template <typename T>
inline
bool between (T _, T lo, T hi)
{
	return (_ >= lo and _ <= hi);
} 

#endif
